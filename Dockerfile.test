# Use the locally built base image
FROM localhost/trading-bot-base:latest

WORKDIR /app

# Copy application code and tests
COPY src/ ./src/
COPY tests/ ./tests/

# Command to run tests
CMD ["python", "-m", "pytest"] 