services:
  trading-bot:
    build: .
    volumes:
      - .:/app
      - ./secrets.json:/app/secrets.json:ro
    environment:
      - PYTHONPATH=/app/src
      - TRADING_MODE=simulation
      - SECRETS_FILE=/app/secrets.json
    command: python -m local_run 

  test:
    build:
      context: .
      dockerfile: Dockerfile.test
    volumes:
      - ./src:/app/src
      - ./tests:/app/tests
    command: python -m pytest
    profiles:
      - test 